# Screenshots

Images embedded in the top-level [`README.md`](../../README.md), captured from
the live deploy at [mat.umai-tech.com](https://mat.umai-tech.com).

| File | Page |
| --- | --- |
| `landing.png` | Landing page (`/`). |
| `chat.png` | Chat app (`/chat`). |
| `settings.png` | Settings drawer — the four provider key fields. |
| `architecture.png` | An architecture detail page (`/architectures/v3`). |

To refresh one, just overwrite the file (keep the name) and the README picks it up.
